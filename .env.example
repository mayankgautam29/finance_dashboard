# Next.js (local + Vercel). Do not commit a real `.env` with secrets.
#
# Vercel: set BACKEND_URL in Project → Settings → Environment Variables.
# Use the public HTTPS URL of your Railway FastAPI service (no trailing slash).
# Example: https://finance-dashboard-api-production.up.railway.app
BACKEND_URL=http://127.0.0.1:8000
